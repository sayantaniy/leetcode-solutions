/**
 * @param {number[]} nums1
 * @return {boolean}
 */
var uniformArray = function(nums1) {
    //3 situations
    //all odd
    //all even
    //mix of odd and even, even-odd=odd, so an odd array can always be contructed
    //always true
    return true
};